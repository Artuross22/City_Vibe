using City_Vibe.Models;

namespace City_Vibe.Interfaces
{
    public interface ISaveEventsRepository
    {
        //bool Add(SaveAnInterestingEvent eventAdd);
        //bool Update(SaveAnInterestingEvent eventUp);
        //bool Delete(SaveAnInterestingEvent eventDelete);
        //bool Save();
    }
}
